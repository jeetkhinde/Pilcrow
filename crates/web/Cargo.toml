[package]
name = "pilcrow-web"
version = "0.1.0"
edition = "2021"
description = "Web/SSR integration layer for Pilcrow applications"
license = "MIT"

[dependencies]
pilcrow-core = { path = "../core" }
runtime = { package = "pilcrow-runtime", path = "../runtime" }
pilcrow-client = { path = "../client" }
pilcrow-macros = { path = "../macros" }
axum = "0.7"
tracing = "0.1"
